export const authStorageKey = 'auth';

export const routeNames = {
  login: 'login',
  register: 'register',
  schedule: 'schedule',
};

export const endpoints = {
  login: '/auth/login',
  register: '/auth/register',
  logout: '/auth/logout',
  practices: '/practices',
};
